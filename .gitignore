# MindMux client runtime artifacts (task logs, etc.) — not part of the product
.mindmux/

# macOS
.DS_Store

# Local brain.md wiring for working ON this repo. The canonical contract + skeleton
# ship in skills/brain-setup/assets/; these root copies are local-only, not product.
/BRAIN.md
/CLAUDE.md

# VS Code / code-server extension build artifacts — derived from extension/src
# and skills/, not checked in. dist/ and the packaged .vsix ARE checked in
# (see extension/dist/ and extension/*.vsix) for run-time testing without a
# build step; assets/ stays derived-only since it's just an intermediate
# staging copy that vsce zips into the .vsix.
/extension/node_modules/
/extension/out/
/extension/assets/
